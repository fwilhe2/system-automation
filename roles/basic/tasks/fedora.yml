---
- name: Install Basic Packages
  become: true
  become_user: root
  dnf:
    state: present
    name: "{{ item.name | default(item) }}"
  loop: "{{ basic_packages['RedHat'] }}"
  when: ansible_os_family == 'RedHat'
