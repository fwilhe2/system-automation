---
- name: Install Basic Packages
  become: true
  become_user: root
  apt:
    state: present
    name: "{{ item.name | default(item) }}"
  loop: "{{ basic_packages['Debian'] }}"
  when: ansible_os_family == 'Debian'
